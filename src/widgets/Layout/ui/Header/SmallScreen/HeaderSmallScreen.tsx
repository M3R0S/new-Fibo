import { FC } from 'react';

import cl from "./HeaderSmallScreen.module.scss"
import { HeaderSmallScreenProps } from '.';

export const HeaderSmallScreen: FC<HeaderSmallScreenProps> = (props) => {
    const {  } = props

	return (
        <div>
            
        </div>
    );
};
