import { createSlice } from "@reduxjs/toolkit";

import { {{pascalCase}}State } from './types'

const initialState: {{pascalCase}}SliceState = {

}

export const {{camelCase}}Slice = createSlice({
    name: "{{camelCase}}",
    initialState,
    reducers: {

    }
})

export const {  } = {{camelCase}}Slice.actions